services:
  abl-builder:
    build:
      context: .
      dockerfile: Dockerfile
    image: gbl_builder:latest
    volumes:
      - .:/workspace
      - ${NDK_PATH:-/tmp/ndk}:/opt/android-ndk:ro
    command: >
      bash -c "make clean && make target_toolkit_linux && make target_toolkit_windows"